/**
 * คัดลอกไฟล์นี้เป็น config.js แล้วใส่ URL ของ Apps Script Web App
 *
 * วิธีหา URL: script.google.com → Deploy → Manage deployments → คัดลอก Web App URL (ลงท้าย /exec)
 * ตอน deploy ตั้ง: Execute as = Me, Who has access = Anyone
 */
window.API_URL = "https://script.google.com/macros/s/PASTE_YOUR_DEPLOYMENT_ID_HERE/exec";
window.APP_VERSION = "1.0.0";
